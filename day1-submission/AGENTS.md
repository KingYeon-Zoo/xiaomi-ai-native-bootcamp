# Codex 工作入口

<INSTRUCTIONS>
1. 开始任何规划、开发、审查、测试或提交工作前，必须完整读取 `docs/engineering-constraints.md`。
2. 除根目录 `README.md`、`AGENTS.md` 外，开发过程 Markdown 正文必须位于 `docs/`；课程检查器要求的旧路径只允许作为符号链接入口。
3. 题目原文、FAQ 和课程检查脚本是只读输入；发生冲突时以题目和检查器为准，并把人工判断写入 `docs/ai-log.md`。
4. 未实际执行的结果不得写成 PASS；存在 BLOCKED 时不得提交。
5. 所有训练营文档使用简体中文。
</INSTRUCTIONS>
