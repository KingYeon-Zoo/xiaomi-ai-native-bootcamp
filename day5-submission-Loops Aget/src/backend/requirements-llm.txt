# Lightweight dependencies for the `llm_api` moderation approach.
# No torch / transformers / clip / sentence-transformers — the LLM API
# handles tech-relevance judgement over plain HTTP.
# Install with:  pip install -r requirements-llm.txt

# Web framework + server
fastapi==0.104.1
uvicorn==0.30.6
starlette==0.27.0
python-multipart==0.0.9
aiofiles==23.2.1

# Data validation
pydantic==2.9.2
pydantic-settings==2.5.2
pydantic_core==2.23.4
annotated-types==0.7.0
typing_extensions==4.12.2

# MongoDB
pymongo==4.16.0
dnspython==2.6.1

# HTTP client (used to call the LLM API)
requests==2.32.3
httpx==0.27.0
openai==2.45.0
certifi==2024.12.14
charset-normalizer==3.4.0
idna==3.10
urllib3==2.2.3

# Stateful agent orchestration + MongoDB checkpoints
langgraph==1.2.9
langgraph-checkpoint-mongodb==0.4.0

# Config + utilities
python-dotenv==1.0.1
loguru==0.7.2
colorama==0.4.6

# Image handling (uploads are saved/served, not ML-analyzed)
pillow==10.4.0

# Tests
pytest==8.3.3
pytest-asyncio==0.24.0
