# TripSplit · 测试矩阵

> 六类场景全覆盖、失败可复现、改动必回归。

---

## 测试总览

| 维度 | 数量 | 工具 |
|------|------|------|
| 后端单元测试 | 10 | pytest |
| 后端集成测试 | 15 | pytest + httpx |
| 前端组件测试 | 15 | vitest + @testing-library/react |
| 手动 e2e | 10 步 | 浏览器手动验证 |
| **合计** | **40 + 10 步** | — |

---

## 后端 pytest（25 条）

### test_trips.py — 旅行 CRUD（8 条）

| 场景 | 期望 | 类型 |
|------|------|------|
| 创建旅行（正常） | 200，返回含 id 的 trip | 正常 |
| 创建旅行（名称为空） | 400 | 边界 |
| 创建旅行（日期倒置） | 400 | 边界 |
| 获取旅行列表 | 200，返回数组 | 正常 |
| 获取旅行详情 | 200，返回完整数据 | 正常 |
| 获取详情（不存在） | 404 | 失败 |
| 删除旅行 | 204 | 正常 |
| 删除旅行（不存在） | 404 | 失败 |

### test_members.py — 成员管理（3 条）

| 场景 | 期望 | 类型 |
|------|------|------|
| 添加成员（正常） | 200，members 含新成员 | 正常 |
| 添加成员（空名） | 400 | 边界 |
| 添加成员（重复名） | 400 | 边界 |

### test_expenses.py — 账单 CRUD（9 条）

| 场景 | 期望 | 类型 |
|------|------|------|
| 添加账单（正常） | 200 | 正常 |
| 添加账单（金额为 0） | 400 | 边界 |
| 添加账单（金额为负） | 400 | 边界 |
| 添加账单（payer 不在 members） | 400 | 失败 |
| 添加账单（participants 为空） | 400 | 边界 |
| 编辑账单（部分字段更新） | 200，未传字段保留原值 | 正常 |
| 删除账单 | 204 | 正常 |
| 编辑后 settlement 清除 | settlement 为 null | 正常 |
| 删除后 settlement 清除 | settlement 为 null | 正常 |

### test_settle.py — 结算算法（5 条）

| 场景 | 期望 | 类型 |
|------|------|------|
| 3 人均摊 | 小王+440，小李-60，小张-380 | 正常 |
| 2 人场景 | B→A 50 | 正常 |
| 浮点精度（100.01/3） | round 后余额归零 | 边界 |
| 总额对账 | 转账总额 = 账单总额 | 来源 |
| 余额归零 | 所有 balance 之和 = 0 | 来源 |

---

## 前端 vitest（15 条）

### TripForm.test.jsx（2 条）

| 场景 | 期望 | 类型 |
|------|------|------|
| 创建成功 | onSubmit 被调用 | 正常 |
| 名称为空拒绝 | onSubmit 不调用，显示错误 | 边界 |

### MemberForm.test.jsx（3 条）

| 场景 | 期望 | 类型 |
|------|------|------|
| 添加成功 | onSubmit 被调用 | 正常 |
| 空名拒绝 | 显示"成员名不能为空" | 边界 |
| 重复名拒绝 | 显示"成员名已存在" | 边界 |

### ExpenseForm.test.jsx（4 条）

| 场景 | 期望 | 类型 |
|------|------|------|
| 添加成功 | onSubmit 被调用 | 正常 |
| 金额为 0 拒绝 | 显示"金额必须大于 0" | 边界 |
| 金额为负拒绝 | 同上 | 边界 |
| 参与人为空拒绝 | 显示"至少选择一个参与人" | 边界 |

### ExpenseList.test.jsx（3 条）

| 场景 | 期望 | 类型 |
|------|------|------|
| 列表渲染 | 渲染 3 个列表项 | 正常 |
| 编辑回调 | onEdit 被调用 | 正常 |
| 删除回调 | onDelete 被调用 | 正常 |

### Settlement.test.jsx（3 条）

| 场景 | 期望 | 类型 |
|------|------|------|
| 结果渲染 | 成员统计 + 转账方案正确 | 正常 |
| 余额颜色（正数） | class = positive | 正常 |
| 余额颜色（负数） | class = negative | 正常 |

---

## 手动 e2e（10 步）

| 步 | 操作 | 预期 |
|----|------|------|
| 1 | 创建旅行"南京 3 日游" | 出现在列表 |
| 2 | 添加成员：小王、小李、小张 | 成员列表展示 3 人 |
| 3 | 添加账单：酒店 900（小王付，3 人均摊） | 账单列表展示 |
| 4 | 添加账单：晚饭 360（小李付，3 人均摊） | 账单列表展示 |
| 5 | 添加账单：打车 80（小张付，小王/小张） | 账单列表展示 |
| 6 | 查看成员统计 | 小王+440，小李-60，小张-380 |
| 7 | 点击生成结算 | 转账方案：小张→小王 380，小李→小王 60 |
| 8 | 编辑酒店金额为 800 | settlement 清除 |
| 9 | 删除打车账单 | 列表剩 2 条 |
| 10 | 删除旅行 | 列表为空 |

---

## 回归用例

改结算算法后，`test_settle.py` 5 条全跑一遍。
改 API 校验后，`test_expenses.py` 9 条全跑一遍。
断言状态不变 ✅

---

## 复现说明

每条用例固定输入（conftest.py 初始化临时 trips.json），失败可稳定重现，不靠运气。
