# AI 协作决策记录 — task-manager Bug 修复

> 每条记录包含五个字段。最关键的是「人工判断」——不是"AI说的对"，而是你采纳/修改/拒绝了AI的什么建议、为什么。

---

## 第 1 条

| 字段 | 内容 |
|------|------|
| **目的** | 定位并修复 Bug 1（字符串 priority 排序异常），并生成修复记录 |
| **输入** | `test.py` 输出（测试 3 失败）、`src/task_manager.py` 源码、当前 docs 模板要求 |
| **建议** | AI 建议先在 `add_task()` 中统一将 `priority` 转为 `int`，转换失败则抛出 `ValueError`；随后为满足测试行为，补充 `priority_aliases` 映射并回退到默认值 |
| **人工判断** | 采纳核心方向（统一类型转换），但拒绝直接抛错方案，改为“映射+默认值”，以兼顾现有测试和函数鲁棒性 |
| **验证** | 重新运行 `python test.py`，Bug 1 对应断言由失败变为通过（结果：5 通过 / 2 失败） |

---

## 第 2 条

| 字段 | 内容 |
|------|------|
| **目的** | 定位并修复 Bug 2（空字符串/纯空格 title 未抛异常），并更新 docs 记录 |
| **输入** | `test.py` 输出（测试 2 失败）、`src/task_manager.py` 源码、Bug 2 复现结果和根因分析 |
| **建议** | AI 建议在 `trimmed_title = title.strip()` 后检查是否为空，为空则抛 `ValueError`，并运行 `python test.py` 验证回归 |
| **人工判断** | 采纳该方案，因为它符合“最小修复”要求，且与测试期望一致；没有额外改动其它逻辑 |
| **验证** | 重新运行 `python test.py`，全部 7 个测试通过（0 失败），Bug 2 断言由失败变为通过 |
