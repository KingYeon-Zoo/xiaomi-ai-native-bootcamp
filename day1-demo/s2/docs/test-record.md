# 测试记录 — task-manager

> 三栏格式：输入 / 预期输出 / 实际输出。不用跑代码就能验证。

---

## 测试 1：正常输入

| 输入 | 预期输出 | 实际输出 |
|------|---------|---------|
| `add_task([], '学习 RAG', 1)` | 列表含 1 个任务，title='学习 RAG', priority=1 | |
| 依次添加 `('低优先',1)`, `('高优先',5)`, `('中优先',3)` | 顺序：高优先→中优先→低优先 | |

---

## 测试 2：边界输入（Bug 2：空 title）

| 输入 | 预期输出 | 实际输出 |
|------|---------|---------|
| `add_task([], '', 1)` | 抛出 `ValueError`，且错误信息包含 "title" | ✅ 修复后抛出 `ValueError('title must not be empty')`，测试 2 断言通过 |
| `add_task([], '   ', 1)` | 抛出 `ValueError`，且错误信息包含 "title" | ✅ 修复后抛出 `ValueError('title must not be empty')`，测试 2 断言通过 |

---

## 测试 3：异常输入（Bug 1：字符串 priority）

| 输入 | 预期输出 | 实际输出 |
|------|---------|---------|
| `add_task([], '重要任务', 'high')` 后接 `add_task([], '普通任务', 'low')` | `priority` 被转换为数字，不存在字符串类型；测试 3 断言通过 | ✅ 修复后 `priority` 为 `int`，`test.py` 中测试 3 通过（`5 通过, 2 失败`，剩余失败为 Bug 2） |

---

## 补全说明

- 先运行 `python3 test.py`，观察哪些测试失败
- 定位Bug根因后，填写上表中 `[补全]` 的预期输出和实际输出
- 修复Bug后重新跑测试，更新实际输出列为修复后的结果
