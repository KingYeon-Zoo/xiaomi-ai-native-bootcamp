# 项目级课程兼容入口

1. 在执行项目任务前，必须完整读取 `docs/engineering-constraints.md`。
2. `docs/engineering-constraints.md` 是工程约束唯一正文；本文件不得复制另一套规则。
3. 除根目录 `README.md`、`AGENTS.md` 和本文件外，所有开发过程文件必须使用 Markdown 并写入 `docs/` 或其子目录。
4. 如果 `docs/` 不存在，先创建；如果工程约束正文缺失，先生成正文，再开始开发。
5. 所有训练营文档使用简体中文。
