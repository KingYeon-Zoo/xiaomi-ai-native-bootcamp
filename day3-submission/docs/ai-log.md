# Day3 AI 协作与人工判断日志

> 仅记录本次 Day3 submission 的真实协作。标记含义：🔴 拒绝，🟡 修改后采纳，🟢 有条件采纳。

### 记录 1：确认任务范围 🟢

- **目的**：确定 Day3 必交内容。
- **输入**：`../day3-demo/任务.txt` 与 demo 文件树。
- **建议**：以八份交付文档为主线，同时保留可运行项目作为证据。
- **人工判断**：有条件采纳；八份文档必须齐全，但不能只查文件名，还要重新验证代码。
- **验证**：`docs/交付文档/` 存在题目指定的 8 个 Markdown。

### 记录 2：选择个人项目模式 🟢

- **目的**：选择训练营 skill 的交付规范。
- **输入**：用户要求“自己的 day3 submission”，没有成员分工或会议材料。
- **建议**：按个人项目最小闭环执行。
- **人工判断**：采纳；不制造团队分工、会议或共同决策。
- **验证**：`docs/engineering-constraints.md` 明确项目类型为个人课程练习。

### 记录 3：处理老师 demo 来源 🟡

- **目的**：兼顾“demo 正确”和“自己的提交”。
- **输入**：用户明确说明 demo 是老师提供且文件正确。
- **建议**：复制 demo 后直接提交。
- **人工判断**：修改后采纳；复用实现，但增加来源说明、个人验证与复盘，不声称代码原创。
- **验证**：README、工程约束和复盘均写明贡献边界。

### 记录 4：是否改写业务代码 🔴

- **目的**：判断是否需要通过改代码体现个人工作。
- **输入**：demo 测试完整，用户确认文件正确。
- **建议**：可换主题、框架或增加功能形成差异。
- **人工判断**：拒绝；Day3 关注产物流转，额外改造会引入风险且偏离任务。
- **验证**：业务功能范围保持 TripSplit PRD，不加入登录、云同步或真实支付。

### 记录 5：采用八文档顺序 🟢

- **目的**：建立可追溯的交付链。
- **输入**：题目指定的完整流程链。
- **建议**：按“流转图→PRD→方案→开发→测试→Gate→三件套→Token”组织。
- **人工判断**：采纳，并让横向文档引用工程约束和实际测试记录。
- **验证**：`docs/交付文档/project-flow-map.md` 展示依赖关系。

### 记录 6：保留 Web 前后端方案 🟢

- **目的**：复核架构取舍。
- **输入**：CLI、Web、小程序三方案与两天交付约束。
- **建议**：保留 React + FastAPI Web 方案。
- **人工判断**：有条件采纳；展示性与风险平衡合理，但明确 JSON 只适合单机场景。
- **验证**：`design-options.md` 包含选择、不选理由及并发风险。

### 记录 7：限制功能范围 🔴

- **目的**：防止 AI 过度设计。
- **输入**：PRD 的 8 条非目标和现有完整闭环。
- **建议**：可加入多币种、OAuth、数据库和消息推送。
- **人工判断**：拒绝本次实现；这些只可作为未来方向，不能进入 Day3 验收。
- **验证**：工程约束“非目标”与 PRD 一致。

### 记录 8：重新执行后端测试 🟢

- **目的**：将 demo 历史结论转换为个人可复核证据。
- **输入**：`backend/requirements.txt` 与 4 个 pytest 文件。
- **建议**：创建隔离虚拟环境并运行全部测试。
- **人工判断**：采纳；不复用文档中的历史 PASS 文案。
- **验证**：本次执行得到 `25 passed in 0.21s`，见 `docs/test-record.md`。

### 记录 9：重新执行前端测试 🟢

- **目的**：确认组件测试在当前环境可复现。
- **输入**：`package-lock.json` 与 5 个 Vitest 文件。
- **建议**：用 `npm ci` 固定依赖，再运行 `npm test -- --run`。
- **人工判断**：采纳；比 `npm install` 更适合提交复核。
- **验证**：本次得到 5 files、15 tests passed。

### 记录 10：记录启动失败 🟡

- **目的**：处理后端冒烟测试首次失败。
- **输入**：在 `backend/` 目录执行了带 `backend/.venv` 前缀的命令。
- **建议**：检查端口、依赖和应用代码。
- **人工判断**：先检查命令上下文，确认是重复路径而非应用缺陷；改用 `.venv/bin/python`。
- **验证**：复测 `GET /trips` 返回 `{"trips":[]}`，失败与修正均记入测试记录。

### 记录 11：补充生产构建 🟢

- **目的**：避免只验证测试环境而忽略打包错误。
- **输入**：Vite 前端项目与已安装依赖。
- **建议**：执行 `npm run build`。
- **人工判断**：采纳，作为 README 可复现性的补充证据。
- **验证**：33 个模块转换完成，生产构建成功。

### 记录 12：处理手动 E2E 状态 🔴

- **目的**：修正文档里的证据边界。
- **输入**：demo 文档写有“手动 E2E 通过”，本次没有实际走浏览器 10 步。
- **建议**：沿用 demo 的通过状态。
- **人工判断**：拒绝；历史结果不能冒充本次个人执行。
- **验证**：`test-strategy.md`、`qa-gates.md` 和 `test-record.md` 均明确“本次未执行”。

### 记录 13：生成工程约束入口 🟢

- **目的**：让后续 Agent 先读规则再修改项目。
- **输入**：训练营 skill 的模板与本项目事实。
- **建议**：生成根目录 `AGENTS.md` 和 `docs/engineering-constraints.md`。
- **人工判断**：采纳；完整规则只放在 docs，根目录只路由。
- **验证**：两个文件存在，且工程约束无模板占位符。

### 记录 14：保护 GitLab 凭据 🟢

- **目的**：在提交时使用 `.env` token 且不泄露。
- **输入**：根目录 `.env` 为单行裸 token。
- **建议**：只在命令进程变量中读取，通过请求头使用，不写入远端或日志。
- **人工判断**：采纳，并确认提交目录 `.gitignore` 不会包含根目录 `.env`。
- **验证**：GitLab 查询仅输出 HTTP 状态，不输出 token；目标项目初查为 404。

### 记录 15：提交前双重审计 🟢

- **目的**：满足题目检查器与训练营 skill 两套门禁。
- **输入**：`check-submission.sh` 和 `audit-xiaomi-project.sh`。
- **建议**：先跑项目专用检查器，再跑 personal 模式审计，存在 BLOCKED 就停止推送。
- **人工判断**：采纳；审计结果必须补入 `docs/test-record.md` 后再提交。
- **验证**：专用检查器评级 PASS（62 PASS、1 WARNING、0 BLOCKED）；训练营审计评级 PASS（39 PASS、0 WARNING、0 BLOCKED）。

### 记录 16：修正 Git 推送认证方式 🟡

- **目的**：在不把 token 写入远端 URL 的前提下完成 Git 推送。
- **输入**：GitLab API 使用 token 成功，但 Git HTTP Bearer 认证返回 Access denied。
- **建议**：把 token 直接嵌入远端 URL。
- **人工判断**：拒绝持久化凭据；改用仅对当前进程生效的 PAT Basic 请求头。
- **验证**：`main -> main` 推送成功，`git remote -v` 仍为无凭据 HTTPS URL。
