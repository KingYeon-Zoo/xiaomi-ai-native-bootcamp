# AI Log — Day2 学习任务清单助手

## 第 1 条：状态存储选择

| 字段 | 内容 |
|------|------|
| **目的** | 选择任务进度的持久化方式。 |
| **输入** | 单用户 CLI、五个固定文件、无需历史记录的 Spec。 |
| **建议** | AI 建议 SQLite，便于以后增加时间戳和多人。 |
| **人工判断** | 拒绝 SQLite，采用 JSON。数据库和扩展字段都属于非目标，JSON 更容易人工检查。 |
| **验证** | 状态文件只有五个布尔字段，8 条测试通过。 |

## 第 2 条：大小写失败修复

| 字段 | 内容 |
|------|------|
| **目的** | 修复 `readme.MD` 被当作未知文件。 |
| **输入** | 测试显示预期退出码 0、实际为 2，其余 7 条均通过。 |
| **建议** | AI 建议把所有任务键改成小写。 |
| **人工判断** | 修改建议：只在比较时小写，输出和状态仍保留规范名称 `README.md`，避免文档与状态键不一致。 |
| **验证** | 最小修改 `submit_task()` 后 8/8 测试通过，测试记录保留修复前证据。 |

## 第 3 条：失败输出边界

| 字段 | 内容 |
|------|------|
| **目的** | 决定错误时是否直接展示 Python 堆栈。 |
| **输入** | Eval 的工具失败类型和 AGENTS.md 失败转译要求。 |
| **建议** | AI 建议保留堆栈便于调试。 |
| **人工判断** | 用户输出不展示堆栈，只给中文原因和非零退出码；开发者可通过单元测试定位。 |
| **验证** | 未知文件、重复提交、损坏状态三类输出均为人类可读文本。 |
